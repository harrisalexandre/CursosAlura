Classes, Objetos, Atributos e métodos.
-Uma classe define um TIPO de objeto, representa uma estrutura de dados: atributos, construtores, metodos.

Classe possui atributos.
Carro > carroTestar

Atributos: São as características de um objeto, tambem chamados de variaveis > nome, idade, peso, altura.

Metodo: Ações que um objeto pode realizar.  falar(), correr();

-Objeto: uma instancia criada a partir de uma determinada classe.
Carro > carro teste > chama metodo main

**Instanciar = declarar > possivel declarar valores de atributos e chamar metodos.




Construtores: São métodos responsáveis por criar o objeto, instanciando a classe que foi definida.